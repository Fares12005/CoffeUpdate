
export enum ProviderEnum {
    LOCAL = 'local',
    GOOGLE = 'google',
}

export enum UserRoleEnum {
    USER = 'user',
    ADMIN = 'admin'
}

export enum Reserved_StautsEnum {
    CONFIRMED = 'confirmed',
    CANCELLED = 'cancelled',
    EXPIRED = 'expired',
    NON_RESERVED = 'non_reserved'
}